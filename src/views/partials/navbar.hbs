<nav class="bg-white shadow-lg sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            <!-- Logo -->
            <div class="flex items-center">
                <a href="#home" class="flex items-center space-x-3">
                    <div class="w-10 h-10 bg-gradient-to-r from-blue-500 to-indigo-600 rounded-xl flex items-center justify-center">
                        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                        </svg>
                    </div>
                    <span class="text-xl font-bold text-gray-900">DMS System</span
                </a>
            </div>

            <!-- Desktop Navigation -->
            <div class="hidden md:flex items-center space-x-8">
                <a href="#home" class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium transition duration-200">
                    Home
                </a>
                <a href="#features" class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium transition duration-200">
                    Features
                </a>
                <a href="#pricing" class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium transition duration-200">
                    Pricing
                </a>
                <a href="#about" class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium transition duration-200">
                    About
                </a>
                <a href="#contact" class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium transition duration-200">
                    Contact
                </a>
            </div>

            <!-- Auth Buttons / User Menu -->
            <div class="hidden md:flex items-center space-x-4">
                {{#if user}}
                    <!-- User Menu (เมื่อ login แล้ว) -->
                    <div class="relative">
                        <button id="navbar-user-button" onclick="toggleNavbarUserDropdown()" class="flex items-center space-x-3 text-gray-700 hover:text-gray-900 focus:outline-none bg-gray-50 hover:bg-gray-100 px-3 py-2 rounded-lg transition duration-200">
                            <div class="w-8 h-8 bg-gradient-to-r from-blue-500 to-indigo-600 rounded-full flex items-center justify-center">
                                {{#if user.firstName}}
                                    <span class="text-white text-sm font-medium">{{substring user.firstName 0 1}}{{substring user.lastName 0 1}}</span>
                                {{else}}
                                    <i class="fas fa-user text-white text-sm"></i>
                                {{/if}}
                            </div>
                            <div class="text-left">
                                <p class="text-sm font-semibold">
                                    {{#if user.firstName}}
                                        {{user.firstName}} {{user.lastName}}
                                    {{else}}
                                        {{user.username}}
                                    {{/if}}
                                </p>
                            </div>
                            <i class="fas fa-chevron-down text-xs"></i>
                        </button>

                        <!-- User Dropdown Menu -->
                        <div id="navbar-user-dropdown" class="hidden absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-lg border border-gray-200 z-50">
                            <div class="py-2">
                                <!-- Profile Info -->
                                <div class="px-4 py-2 border-b border-gray-100">
                                    <p class="text-sm font-semibold text-gray-900">
                                        {{#if user.firstName}}
                                            {{user.firstName}} {{user.lastName}}
                                        {{else}}
                                            {{user.username}}
                                        {{/if}}
                                    </p>
                                    <p class="text-xs text-gray-500">{{user.email}}</p>
                                </div>
                                
                                <!-- Menu Items -->
                                <a href="/backend/dashboard" class="flex items-center space-x-3 px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 transition duration-200">
                                    <i class="fas fa-tachometer-alt w-4"></i>
                                    <span>Dashboard</span>
                                </a>
                                
                                <a href="/backend/profile" class="flex items-center space-x-3 px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 transition duration-200">
                                    <i class="fas fa-user-circle w-4"></i>
                                    <span>Profile</span>
                                </a>
                                
                                <a href="/backend/settings" class="flex items-center space-x-3 px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 transition duration-200">
                                    <i class="fas fa-cog w-4"></i>
                                    <span>Settings</span>
                                </a>
                                
                                <div class="border-t border-gray-100 my-1"></div>
                                
                                <a href="/auth/logout" class="flex items-center space-x-3 px-4 py-2 text-sm text-red-600 hover:bg-red-50 transition duration-200" onclick="return confirm('คุณต้องการออกจากระบบหรือไม่?')">
                                    <i class="fas fa-sign-out-alt w-4"></i>
                                    <span>Logout</span>
                                </a>
                            </div>
                        </div>
                    </div>
                {{else}}
                    <!-- Auth Buttons (เมื่อยังไม่ login) -->
                    <a href="/auth/login" class="text-gray-700 hover:text-blue-600 px-4 py-2 rounded-lg text-sm font-medium transition duration-200">
                        Sign In
                    </a>
                    <a href="/auth/register" class="bg-gradient-to-r from-blue-500 to-indigo-600 text-white px-6 py-2 rounded-lg text-sm font-medium hover:from-blue-600 hover:to-indigo-700 transition duration-200 transform hover:scale-105">
                        Get Started
                    </a>
                {{/if}}
            </div>

            <!-- Mobile menu button -->
            <div class="md:hidden">
                <button onclick="toggleMobileMenu()" class="text-gray-700 hover:text-blue-600 focus:outline-none focus:text-blue-600">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                    </svg>
                </button>
            </div>
        </div>

        <!-- Mobile Navigation -->
        <div id="mobile-menu" class="hidden md:hidden">
            <div class="px-2 pt-2 pb-3 space-y-1 bg-white border-t border-gray-200">
                <a href="/" class="block text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-base font-medium">
                    Home
                </a>
                <a href="#features" class="block text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-base font-medium">
                    Features
                </a>
                <a href="#pricing" class="block text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-base font-medium">
                    Pricing
                </a>
                <a href="#about" class="block text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-base font-medium">
                    About
                </a>
                <a href="#contact" class="block text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-base font-medium">
                    Contact
                </a>
                
                {{#if user}}
                    <!-- Mobile User Menu (เมื่อ login แล้ว) -->
                    <div class="pt-4 pb-3 border-t border-gray-200">
                        <div class="flex items-center px-3 py-2">
                            <div class="w-10 h-10 bg-gradient-to-r from-blue-500 to-indigo-600 rounded-full flex items-center justify-center mr-3">
                                {{#if user.firstName}}
                                    <span class="text-white text-sm font-medium">{{substring user.firstName 0 1}}{{substring user.lastName 0 1}}</span>
                                {{else}}
                                    <i class="fas fa-user text-white text-sm"></i>
                                {{/if}}
                            </div>
                            <div>
                                <div class="text-base font-medium text-gray-900">
                                    {{#if user.firstName}}
                                        {{user.firstName}} {{user.lastName}}
                                    {{else}}
                                        {{user.username}}
                                    {{/if}}
                                </div>
                                <div class="text-sm text-gray-500">{{user.email}}</div>
                            </div>
                        </div>
                        <div class="mt-3 space-y-1">
                            <a href="/backend/dashboard" class="block text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-base font-medium">
                                <i class="fas fa-tachometer-alt mr-2"></i>Dashboard
                            </a>
                            <a href="/backend/profile" class="block text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-base font-medium">
                                <i class="fas fa-user-circle mr-2"></i>Profile
                            </a>
                            <a href="/backend/settings" class="block text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-base font-medium">
                                <i class="fas fa-cog mr-2"></i>Settings
                            </a>
                            <a href="/auth/logout" class="block text-red-600 hover:text-red-800 px-3 py-2 rounded-md text-base font-medium" onclick="return confirm('คุณต้องการออกจากระบบหรือไม่?')">
                                <i class="fas fa-sign-out-alt mr-2"></i>Logout
                            </a>
                        </div>
                    </div>
                {{else}}
                    <!-- Mobile Auth Buttons (เมื่อยังไม่ login) -->
                    <div class="pt-4 pb-3 border-t border-gray-200">
                        <div class="flex flex-col space-y-3">
                            <a href="/auth/login" class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-base font-medium">
                                Sign In
                            </a>
                            <a href="/auth/register" class="bg-gradient-to-r from-blue-500 to-indigo-600 text-white px-3 py-2 rounded-md text-base font-medium text-center">
                                Get Started
                            </a>
                        </div>
                    </div>
                {{/if}}
            </div>
        </div>
    </div>
</nav>
